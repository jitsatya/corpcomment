## Accessing and Updating element properties using the event object

We can use event object to update various properties of the element that triggered the event.
like we can use event.currentTarger.disabled = true to disable the button once the button is clicked
